# 阶段 5 DoD 验收报告 #3（真机反馈 P5-5、P5-6 修复后复测）

| 项目 | 内容 |
|---|---|
| 被测阶段 | 阶段 5 — 注入式 UI |
| 验收依据 | `docs/phases/phase-5-inject-ui.md` |
| 被测提交 | `fix: 段落悬停按钮点不到、尺寸偏小 (v0.5.2)`（分支 `v0.5-render-inject-ui-hotkeys`） |
| 前两轮 | [DoDR-1](DoDR-1.md)、[DoDR-2](DoDR-2.md) |
| 验收日期 | 2026-07-31 |
| 环境 | Node 24.14.0 / WXT 0.19.29 / TypeScript 5.9.3 / jsdom 29.1.1 |
| 验收方式 | DoDR-2 全套回归（单元 41 条含新增 6 条 + 集成 10 条 + 版本号 6 条） |
| **结论** | **通过（代码与产物层面）** — 两项缺陷来自用户真机反馈，前两轮自动化用例未能覆盖。41 / 41 + 10 / 10 + 6 / 6 断言通过 |

---

## 1. 缺陷来源

DoD 第 7 项「鼠标移到段落上浮出按钮，能顺利移动到按钮并点击」在前两轮均判为通过 —— C-5、C-6 两条断言确实覆盖了 200ms 延迟隐藏与"离开段落后移入按钮"的路径。用户实机使用后反馈按钮**根本点不到**，且尺寸偏小。

复查发现自动化用例的盲区在于：它们在 jsdom 里直接派发 `mouseout` 后紧接着派发按钮的 `mouseover`，中间没有真实指针穿越间隙的耗时，也没有真实布局 —— 恰好绕开了两个真正致命的因素。这是 [DoDR-1 §5](DoDR-1.md#5-验收方式与复现) 里已声明的环境限制在具体条目上的兑现，记录在此作为后续用例设计的反面样本。

---

## 2. 缺陷

### P5-5 段落悬停按钮实际点不到（高，DoD 7）

三个原因叠加，任一单独存在都足以让功能不可用。

**其一，200ms 是从「离开段落」起算的硬预算，且中途还会被白白消耗。** `paragraph-btn.ts` 的 `mouseout` 处理器无条件安排隐藏，而按钮定位在段落右边缘外 4px，指针穿越这段间隙时 `mouseover` 打在宿主页面的其它元素上，旧代码在 `closest(DIRECT)` 返回 null 时直接 `return`，**没有 `clearTimeout`**：

```typescript
const el = (e.target as Element)?.closest?.(DIRECT);
if (!el || el.closest('[data-pt-ui="1"]')) return;   // ← 计时器仍在跑
```

于是这 200ms 要同时覆盖"跨过间隙"与"命中按钮"。更糟的是段落**内部**子元素之间的每一次移动都会触发一次 `mouseout`，同样启动倒计时，只是随后的 `mouseover` 命中同一段落才把它清掉 —— 时序上先减后加，稍有抖动就提前隐藏。

**其二，按钮多半根本不在可见区域。** 旧的 `position()` 无条件写 `left = r.right + 4`、`top = r.top`：

```typescript
btn.style.top = `${r.top}px`;
btn.style.left = `${r.right + 4}px`;
```

段落通常撑满内容列宽，全宽布局（X、Medium 正文区、大量博客主题）下 `r.right` 已经接近甚至等于 `innerWidth`，再 +4 即出界；长段落顶部滚出视口时 `r.top` 为负，按钮被丢到视口上方。用户看到的"闪一下就没了"，相当一部分其实是它出现在了屏幕外。

**其三，按钮是 `position: fixed` 而段落随页面滚动**，显示期间滚动一下按钮就与段落错位，旧代码没有任何重定位。

修法：

| 项 | 旧 | 新 |
|---|---|---|
| 隐藏延迟 | 200ms | 500ms（`HIDE_DELAY` 常量） |
| `mouseout` 判据 | 无条件计时 | 读 `relatedTarget`，仍在同一段落内或正移向 `[data-pt-ui="1"]` 时不计时 |
| 定位 | 直接写 `r.right + 4` / `r.top` | 钳制到视口内：右外侧放不下就落到段落右上角内侧，`top` 夹在 `[4, vh - h - 4]` |
| 滚动 | 不处理 | 显示期间监听 `scroll`（capture + passive）与 `resize` 重新贴合 |
| 卸载 | 只 `unmountIsolated` | 同时摘除 document 与 window 上的四个监听器 |

延迟从阶段文档给的 200ms 改到 500ms 是有意偏离：文档把 200ms 作为"给用户从段落移动到按钮的时间"的示例值，真机上偏紧。500ms 在"够得着"与"不黏手"之间取中，且配合 `relatedTarget` 判据后，段落内部移动不再吃掉预算，实际可用时间比字面值更宽裕。

### P5-6 按钮可点击尺寸偏小（中）

`injected.css` 原为 `font-size: 8px` + `padding: 3px 7px`，实测约 20×15px，指针需要瞄准才点得中，与 P5-5 叠加后体感是"要么点不到要么点不中"。

```
font-size   8px  → 12px
padding     3px 7px → 4px 8px
min-width / min-height  新增 24px
letter-spacing / text-transform  移除（单字「译」不需要字距与大小写变换）
```

24px 是可点击目标的常用下限。字号提到 12px 后 `--pt-font-mono` 的字形也更清晰。

---

## 3. 回归结果

段落按钮组由 9 条扩到 15 条，新增 6 条全部针对本轮缺陷：

```
C-5    离开段落后 300ms 仍可见、600ms 后隐藏（500ms 延迟生效）
C-6    离开段落后移入按钮，700ms 后仍可见且可点击
C-6b   段落内子元素之间移动（relatedTarget 仍在段落内）不启动倒计时
C-10   段落 right === innerWidth 时，按钮 left 被钳制在 [0, vw - 24]
C-11   段落 top === -800 时，按钮 top ≥ 0
C-12   scroll 事件后按钮 top 由 300px 重贴合到 120px
C-13   卸载后 document 级 mouseover/mouseout 监听已摘除
C-14   injected.css 中 min-width / min-height 均为 24px
```

其余各组本轮未改动，逐条回归与 DoDR-2 一致：

| 组 | 结果 |
|---|---|
| A shadow 隔离（6 条） | 6 / 6 ✅ |
| B 悬浮球（12 条） | 12 / 12 ✅ |
| C 段落按钮（15 条） | 15 / 15 ✅ |
| D toast（4 条） | 4 / 4 ✅ |
| E 自翻译与宿主污染（4 条） | 4 / 4 ✅ |
| 集成（10 条） | 10 / 10 ✅ |
| 版本号与产物一致性（6 条） | 6 / 6 ✅ |

`pnpm typecheck` 0 error，`pnpm build` 产物 63.64 kB（+0.78 kB），版本号 0.5.1 → 0.5.2。

---

## 4. 仍需真机自测

本轮缺陷正说明自动化用例覆盖不到真实指针轨迹与真实布局，修复效果**需要你在浏览器里再确认一次**：

- 在全宽布局站点（X、Medium）上 hover 段落，按钮应出现在可见区域内而非屏幕外
- 从段落边缘沿直线移向按钮，中途停顿约半秒仍应可点
- 段落内有链接、加粗等内联元素时，在其间移动按钮不应闪烁或消失
- 显示期间滚动页面，按钮应跟着段落走
- 按钮大小是否合适 —— 24px 是下限，若仍觉小可再往上调，改 `injected.css` 一处即可

---

## 5. 后续调整（v0.5.3）

用户实机试用后要求把隐藏延迟再放宽：`HIDE_DELAY` 500ms → **1500ms**。

500ms 是我按"够得着但不黏手"估的值，实际手感由用户判断更准。偏长的代价是移开鼠标后按钮还会停留一会儿，但它只占段落右上角一小块、不遮挡正文，比"够不着"轻得多。仅此一个常量变化，定位、`relatedTarget` 判据、滚动重贴合与尺寸均未动。

对应断言随之调整并复测通过：

```
C-5    离开段落后 1200ms 仍可见、1700ms 后隐藏
C-6    离开段落后移入按钮，1800ms 后仍可见且可点击
C-6b   段落内子元素之间移动，1800ms 后仍可见
```

全套 139 条断言仍全部通过，`pnpm typecheck` 0 error，产物 63.64 kB。

---

## 6. 后续调整（v0.5.4）—— P5-7 悬停时按钮闪现

用户反馈按钮"闪现"。这一轮改用**真实浏览器**复现：把 `paragraph-btn.ts` 打成 IIFE、配一个含受限列宽与全宽两种版式的 fixture 页，在浏览器面板里驱动真实指针并记录按钮每一次 `display` / 位置变化。

### 成因

不是显示与隐藏在打架，是**按钮在追鼠标**。`mouseover` 一命中段落就立即 `show()` 并重定位，于是指针扫过文章时按钮挨个段落跳。实测日志：

```
指针 a → b → 下方空白
233178 block@964px,20px      ← 弹到 a
240715 block@964px,92px      ← 跳到 b
250231 none                  ← 离开正文才收起
```

真实文章有几十段，一次从上到下的移动就是几十次跳变。§5 把 `HIDE_DELAY` 提到 1.5 秒后这个现象**更显眼** —— 按钮全程挂着，而不是跳完就消失。

### 修法

| 项 | 变更 |
|---|---|
| 悬停意图 | 新增 `SHOW_DELAY = 140ms`，指针在同一段落上停住才浮出；路过时后一次 `mouseover` 重置前一次的计时，一路划过去一次都不弹 |
| 同段落内移动 | `el === target && isVisible()` 时直接返回，不再反复重定位（省掉无谓的强制布局） |
| 淡入 | `opacity` 0 → 1，配 `transition: opacity .12s`，避免硬弹出；段落之间移动时不重新淡入 |
| 全宽版式下的位置 | 右侧放不下时改为浮到段落**上方**的行间空白，不再压在正文第一行上 —— 盖住正在读的那行同样像"闪" |

### 过程中发现的两个隐患

**其一，淡入不能依赖 `requestAnimationFrame`。** 首版把 `opacity = '1'` 写在 rAF 回调里，实测 `document.visibilityState === 'hidden'` 时 **rAF 根本不回调**：

```
{"rAF是否回调":false,"visibilityState":"hidden","btn内联opacity":"0"}
```

content script 天天跑在后台标签页里，这会让按钮卡在全透明 —— 看不见却仍占着点击区域，本身就是一种"时有时无"。改为借 `position()` 里的 `getBoundingClientRect` 强制回流来触发过渡，并在 `visibilityState !== 'visible'` 时直接跳过动画。失败模式从"按钮消失"降级为"没有淡入动画"。

**其二，垂直钳制的取值顺序会反向。** `Math.min(Math.max(top, 4), vh - h - 4)` 在视口比按钮还矮时（极端窄窗、部分嵌入式 webview）会让 `Math.min` 选中负的上限，把按钮推出视口。实测视口塌成 0×0 时算出 `top: -28`。改为 `Math.max(4, Math.min(top, vh - h - 4))`。

### 复测

真实浏览器（1280×720）：

```
快速划过 6 段的显示次数    1      （改前 6）
1.5s 后已收起              none
浮出后内联 opacity         1
受限列宽：按钮在段落右外侧  964 vs 段落 right 960
按钮完全在视口内            true
段落内移动新增重定位        0
全宽段落：按钮浮上方不压正文 true
```

段落按钮组由 15 条扩到 18 条，新增 C-15（悬停意图闸门）、C-16（不依赖 rAF）、C-17（不压正文）。阶段 5 全套 44 条、总计 142 条断言通过，产物 63.93 kB。

### 这轮暴露的方法问题

DoD 第 7 项前两轮判通过、§5 调完仍不对，根因是验收手段一直停在 jsdom：它没有真实指针轨迹、没有布局、`visibilityState` 与 rAF 行为也不同。本轮改用真实浏览器 fixture 后，一次就同时抓到了追鼠标、rAF 失效、钳制反向三个问题。`docs/TESTING.md` 规划的 Playwright 层应当优先覆盖注入 UI 的指针交互，而不是留到最后。
